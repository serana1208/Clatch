<p id="notice"><%= notice %></p>

<h1>年間カレンダー</h1>

<table>
  <thead>
    <tr>
      <th>年度</th>
      <th>カレンダー</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @calendars.each do |calendar| %>
      <tr>
        <td><%= calendar.year %></td>
        <td><%=  %></td> 
        <td><%= link_to 'Show', calendar %></td>
        <td><%= link_to '編集', edit_calendar_path(calendar) %></td>
        <td><%= link_to '削除', calendar, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '登録（教員限定）', new_calendar_path %>
